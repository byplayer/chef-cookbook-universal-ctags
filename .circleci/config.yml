version: 2.1
jobs:
  build:
    machine:
      image: "ubuntu-2004:202010-01"
    steps:
      - checkout
      - run: echo "Hello world!"
      - run: docker version
      # todo install ruby
      # run kitchen test
